generator client {
  provider      = "prisma-client-js"
  binaryTargets = "native"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model BoardInspection1 {
  id                 Int      @id @default(autoincrement())
  date               DateTime @default(now())
  week               Int
  inspectorName      String
  board              String
  problemType        String
  problemDescription String
}

model BoardInventory1 {
  id       Int      @id @default(autoincrement())
  date     DateTime @default(now())
  week     Int
  quantity Int
  board    String
}

model BoardInspection2 {
  id                 Int      @id @default(autoincrement())
  date               DateTime @default(now())
  week               Int
  inspectorName      String
  board              String
  problemType        String
  problemDescription String
}

model BoardInventory2 {
  id       Int      @id @default(autoincrement())
  date     DateTime @default(now())
  week     Int
  quantity Int
  board    String
}
